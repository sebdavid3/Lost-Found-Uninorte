<<<<<<< HEAD
=======
version: '3.8'

>>>>>>> af53bcc1a27495652525bc77a91a9a38d6bbafe8
services:
  db:
    image: postgres:15-alpine
    container_name: lost_found_db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: lost_found_uninorte
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
<<<<<<< HEAD
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin -d lost_found_uninorte"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - lost_found_network
    restart: unless-stopped

  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: lost_found_mq
    ports:
      - '5672:5672'   # Puerto de mensajes
      - '15672:15672' # Panel de administración (user: guest, pass: guest)
    networks:
      - lost_found_network
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "check_running"]
      interval: 10s
      timeout: 5s
      retries: 5

  claims-service:
    build:
      context: ./services/claims-service
      dockerfile: Dockerfile
    container_name: lost_found_claims_service
    ports:
      - '3000:3000'
    environment:
      DATABASE_URL: postgresql://admin:password123@db:5432/lost_found_uninorte?schema=public
      RABBITMQ_URL: amqp://guest:guest@rabbitmq:5672
      NODE_ENV: development
    volumes:
      - ./services/claims-service/src:/usr/src/app/src
      - ./services/claims-service/prisma:/usr/src/app/prisma
    depends_on:
      db:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    networks:
      - lost_found_network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/"]
      interval: 10s
      timeout: 5s
      retries: 3
    command: sh -c "npx prisma migrate deploy && npm run seed:objects && npm run start:prod"

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: lost_found_frontend
    ports:
      - '5173:80'
    depends_on:
      claims-service:
        condition: service_healthy
    networks:
      - lost_found_network
    restart: unless-stopped

  audit-db:
    image: postgres:15-alpine
    container_name: lost_found_audit_db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: audit_log_db
    ports:
      - '5433:5432'
    volumes:
      - audit_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin -d audit_log_db"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - lost_found_network
    restart: unless-stopped

  audit-service:
    build:
      context: ./services/audit-service
      dockerfile: Dockerfile
    container_name: lost_found_audit_service
    ports:
      - '3001:3001'
    environment:
      DATABASE_URL: postgresql://admin:password123@audit-db:5432/audit_log_db?schema=public
      RABBITMQ_URL: amqp://guest:guest@rabbitmq:5672
      NODE_ENV: development
    volumes:
      - ./services/audit-service/src:/usr/src/app/src
      - ./services/audit-service/prisma:/usr/src/app/prisma
    depends_on:
      audit-db:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    networks:
      - lost_found_network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3001/audit-log"]
      interval: 10s
      timeout: 5s
      retries: 3
    command: sh -c "npx prisma migrate deploy && npm run start:prod"

networks:
  lost_found_network:
    driver: bridge

volumes:
  postgres_data:
  audit_postgres_data:
=======
    restart: unless-stopped

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: lost_found_backend
    ports:
      - '3000:3000'
    environment:
      - DATABASE_URL=postgresql://admin:password123@db:5432/lost_found_uninorte?schema=public
    depends_on:
      - db
    volumes:
      - ./backend:/usr/src/app
      - /usr/src/app/node_modules
    restart: unless-stopped

volumes:
  postgres_data:
>>>>>>> af53bcc1a27495652525bc77a91a9a38d6bbafe8
